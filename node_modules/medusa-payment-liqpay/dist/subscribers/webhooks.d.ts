import { type SubscriberArgs, type SubscriberConfig } from "@medusajs/medusa";
import { LiqPayCallbackStatusResponse } from "../lib/interfaces/liqpay.interfaces";
export declare const SUPPORTED_EVENTS: string[];
export type WebhookEventData = {
    event: (typeof SUPPORTED_EVENTS)[number];
    data: LiqPayCallbackStatusResponse;
};
export declare const config: SubscriberConfig;
export default function webhookHandle({ data, container, }: SubscriberArgs<WebhookEventData>): Promise<void>;
